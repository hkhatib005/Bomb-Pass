/**
 * House Party design tokens — a warm near-black "late-night game room" palette,
 * shared across every screen so a re-tune only ever happens in one place.
 */
export const colors = {
  bg: '#0B0B10',
  /** Warmer, redder ground used only for the live Bomb Pass round — the room's temperature shifts, not just a badge. */
  liveBg: '#170D0E',
  card: '#181820',
  cardRaised: '#202028',
  line: '#2A2A33',
  ink: '#FAF8F2',
  inkDim: '#B8B4AE',
  inkFaint: '#6E6B68',
  flame: '#FF6B35',
  flameInk: '#2A0F00',
  spark: '#FFD166',
  danger: '#F04452',
  mint: '#54D19D',
  mintInk: '#06261B',
} as const;
